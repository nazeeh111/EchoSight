# Bounded v4 objective correction and coherent raw-clock development check

Coordinator explicitly authorizes correcting the independent review's bounded-gain mathematical defect and replacing the invalid response-axis stress; this is not another threshold/search iteration. Preserve all v1–v3 code, reports and b5de1137125e snapshot bytes. No production changes or promotion. Seeds2609/2621 remain ungenerated.

Use the feasible unconstrained two-column least-squares solution plus four box-edge minima, including corners, with gains g0 in[0,2], g1 in[-2,2]. Evaluate actual residuals, explicitly handle zero columns and numerically singular Gram matrices (det <=64machine-epsilon*A*B); both raw and coarse profiles call the same helper. Validate against independent scipy.optimize.lsq_linear on random, correlated, almost coincident, zero-column and gain-bound cases. Mathematical verification precedes acoustic fitting.

Keep every v3 classification gate, kernel normalization, taper, 107-sample early window, five whole-mixture alignment shifts, waveform cost ratios, support/conditioning gates, source bounds, driver delay bounds, Sobol4096/20/3 search budget and70-evaluation bound. No change to signed y(0) normalization based solely on an algebraic counterexample. No claim of exact global nonlinear optimization. Held records still profile gains/alignment, so their score is held-record profiled compatibility.

Twenty-four case rows maximum: twelve reused fresh-v3 cases (2423/2437, six families) quantify objective-only changes using their exact saved immutable-core observations and baseline; twelve newly rendered DEVELOPMENT cases (2441/2459, same six families) apply independently drawn raw per-record alpha uniformly[1-.0018,1+.0018], recording offset uniformly[.02,.10] seconds and Gaussian noise SD.00012 before fixed.45 gain and16-bit quantization. Render x(receiver time)=underlying waveform((time-offset)/alpha) with fractional interpolation, preserving all existing path/filter/source parameters. These are physical affine-clock recording changes, not perturbations of extracted response axes. They do not simulate non-affine drift or interruptions. Full immutable de8442b input/extraction re-estimates pilot slope/intercept, correlates all repeats, reanchors direct zero and rebuilds the training kernel for each realization. No oracle clock/echo/path inputs enter fitting.

Geometry generation retains independently drawn one common source3mm vector plus one4mm vector per source, and one6mm vector per reused receiver group. Supplied full joint source covariance and shared calibration retained. Effective source-buffer propagation speed remains343.45/1.00035 with declared343±.6; source orientation constant, fixed secondary offset and driver FIR. Raw records may still be spatially unsuitable and their rejected/unavailable status must be retained. New seeds are not chosen by inspecting generated positions/results.

Compare original clipped-gain v3 and exact-gain v4 on identical observations for both old and new case groups. Each new case's immutable baseline raw mapper is run once; all source hypotheses use the same raw bytes. Score baseline and hypothetical warning-withheld geometry with unchanged5degree/15cm matching; every removed true surface counts as a miss. Freeze source/criteria hashes before new generation. Record raw/probe/truth/immutable-core hashes, clock estimation versus rendering truth only after fitting, component support, flags, scores, runtime and rejections. Fixed24case workload, no tuning or repeated search against failed answers.

Stop promotion if a valid single-source, near-reflector, direct-null or receiver-filter control is flagged; any of the seven real near-reflector surfaces is not retained; or useful dual-source false-surface suppression is lost. Finish the predeclared workload to preserve failures, then close the branch on any failed gate. No claim of consumer hardware qualification, robust physical normalization, calibrated model probabilities or improved reconstruction completeness from blanket abstention. If all gates pass, report tradeoffs and exact limitations for independent review only.
