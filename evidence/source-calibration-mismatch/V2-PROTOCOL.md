# Separately frozen stand-off arrangement comparison

The first12-session all-rejected study remains immutable and inconclusive. This second and final bounded arrangement study changes only receiver positions and random seeds. Source patterns, true source centers, source/receiver FIRs, emitted probe, reflection law, clocks/noise/quantization, all calibration code, surveys, partitions and acceptance gates remain unchanged. Immutable3745665 continues to isolate parallel metadata changes. No echo is generated or inspected before freezing this design.

## Placement derivation

For declared mechanical prior s0 and source-radius rho=.15m, the direct path obeys D≥|r−s0|−rho, and the reflected path obeys L≤|r−H(s0)|+rho because reflection preserves distances. Under this simulation's explicit reflected/direct amplitude coefficient.20/.55, the conservative ratio is(.20/.55)*(|r−s0|−rho)/(|r−H(s0)|+rho). This assumes the isolated specular reflection law already declared; it does not guarantee any real material's coefficient or unknown hardware response. Receiver physical positions are the planned positions in this renderer;3mm Gaussian survey noise remains an uncertainty, not a hard bound.

Choose eight training positions as the full Cartesian product x∈[.55,3.8], y∈[1.35,1.65], z∈[.35,2.75]metres. Four held positions are[.7,1.37,.55],[.85,1.63,2.6],[3.6,1.39,.5],[3.5,1.61,2.6]. This predetermined arrangement remains noncoplanar (training centered singular values4.596,3.394,.424m), spans both sides of the source in x/z and keeps the30cm separation-axis diversity. Both reference normals remain independent x/y. All physical points lie on the source side of both planes.

Before rendering, the worst conservative ratios over all12stops are.1400for x-reference and.1077for y-reference, both above the existing.07candidate floor with margin. No actual source error, generating echo delay, realized noise, accepted capture or calibration outcome was used to choose a subset. There are no rejected-position resamples. Fixed seeds2821/2833 determine independent survey/noise/clock draws only. Training/held IDs remain0–7/8–11.

## Bounded execution and decision

Run the same twelve cases: two seeds × single/dual_same_phase/dual_opposite_phase × x/y isolated reference. Use the unmodified calibration route from raw recordings and the unchanged external transfer evaluator. Preserve all proposal/rejection and source/speed/reference ambiguity outputs. The v1runtime/protocol remain separately hashed; v2code freezes before generation. No later arrangement/threshold/phase search follows if single controls fail again. A dual proposal is not automatically false qualification; its model-conditional semantics and unrefitted orthogonal-reference behavior determine the evidence. There is no unique point-center truth for a distributed source. No new hardware or API feature is authorized by this comparison alone.
