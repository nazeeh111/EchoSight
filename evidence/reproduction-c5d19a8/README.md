# Clean GitHub reproduction at c5d19a8

A separate GitHub checkout fetched and checked out **c5d19a8ced43f0692539e34d124376d6f4f20ee7**. HEAD and the remote branch agreed. The working tree was clean before and after execution.

- **249 tests passed in 68.737 seconds** (unittest's measured duration; command wall time is recorded separately).
- Saved-output replay recomputed all 20 original transfer rows and decisions; transfer and promotion remain false.
- The ideal-catalog preflight produced 20/24 and expected exit code 1, preserving the demonstrated infeasibility.
- Production runtime, schemas, acquisition and requirements are byte-identical to the previously independently reviewed ab5ea75 checkpoint. Only evaluation helper/tests and evidence/docs were added.

The existing pinned Python 3.12.14, NumPy 2.3.5 and SciPy 1.18.1 environment was reused. No new installation, raw demo, calibration fit or frozen experiment rerun was needed. Both raw demonstrations, actual HTTP/CLI profile/context/replay and offline schemas remain supported by [the unchanged production checkpoint's executed evidence](../reproduction-ab5ea75/). The full suite here includes its ordinary HTTP and recording tests.

[Commands and exit codes](commands.json), [suite output](full-suite.txt), [environment](environment.txt) and [summary](summary.json) record the exact execution. The summary also identifies larger generated replay outputs and local driver files by hash; these are retained in the task work directory rather than duplicated in Git. [The committed replay package](../calibration-room-transfer/replay/) reproduces that arithmetic directly. A separately recorded asset-download receipt verifies the optional raw archive from GitHub.

These are software and saved-output replay checks, not new physical accuracy measurements or a replacement for any failed scientific evaluation.
