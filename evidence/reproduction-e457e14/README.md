# Clean GitHub reproduction at e457e14

Fetched and detached the existing clean clone at `e457e1488e43518d902e4adbb7aad5ed18f4d5ce`; no tracked modifications before/after. The unchanged pinned local environment passes `pip check`. This is fresh source reproduction in the existing environment, not an independent fresh-download installation.

All201 tests pass in64.733s, including loopback HTTP with native permission. The raw twelve-view CLI demonstration returns partial with6surfaces, including2horizontal, in1.293s. Its refinement-budget diagnostic is retained. Export then replay recomputes exactly matching surfaces, hypotheses, dimensions and original recording hashes; replay runtime2.732s. Tests and demo shared local compute, so these are observed runtimes rather than controlled performance benchmarks. Exact commands, implementation hash and export hash are in checks.json.

This verifies software integration and portable source at the identified commit. The demonstration uses simulated recordings. Frozen synthetic failures, external measured-data failures and absent own-device validation remain unchanged.
