# Independent portable-replay follow-up at e372158

No material finding. The portable adapter at exact commit `e372158d9daa47891b0c5f7106b224b0aca75d05` reproduces the two preserved calculations in an isolated directory without the original study tree. Both fits pass all comparisons, with exactly zero source/speed difference on the installed environment. Its explicit `raw_verified: false` and derived-observation scope are correct.

This was a targeted follow-up to the completed scientific review, not a repeat of the nonlinear mathematics review or the original raw-study review. I reviewed `portable-replay.py`, `portable-inputs.json`, `portable-manifest.json`, `portable-README.md` and `portable-result.json`, then extracted all 13 package files directly from the identified Git commit into `work/review-joint-reference/portable-clean/`. Every extracted byte equals the current inspected package. The seven payload hashes in the original manifest and all nine required/supplementary portable-manifest hashes match. The original reviewed protocol, runner, freeze, precheck, admissions and results also match the retained trial files. The original scientific review is present byte-for-byte at the identified commit. Runtime and test paths have no differences from `38ce3ff`.

I independently verified the compact projection against all 12 original full session/reference/observation files and their frozen hashes. All 48 records match exactly: supplied source and clock convention, complete supplied references, receiver surveys and pose-group IDs, observation timing uncertainty and clock fields, selected candidate delays/uncertainty, capture/candidate IDs, and original recording/waveform hashes. The two original seeds and exact per-reference training/held partitions are retained. All 48 referenced recording hashes match the freeze. This projection check uses originals available locally; it does not imply a clean checkout can verify omitted originals from their hashes alone.

The adapter imports the unchanged frozen `fit_pair`, supplies only the compact pair records and preserved admissions, and reads expected fitted results only after both fits complete. No generating truth or expected fit value enters either estimator. The compact source position is the original supplied nominal position, not generating source truth. The adapter does not run candidate extraction/admission anew, access raw files, generate recordings, tune a solver, add seeds, or alter gates. Comparing the already reviewed admission's candidate identities is appropriate for its stated mathematical replay scope.

The independent replay ran using Python isolated mode (`-I`) with the extracted directory as its working directory and the existing NumPy/SciPy environment. The original `work/source-calibration-mismatch` path does not exist in that extracted directory. The imported runner defines its old-data paths but `fit_pair` does not read them; all required fit data come from the projection. Execution returned code 0, and its entire output equals committed `portable-result.json`. Exact gate checks and the adapter's numerical comparisons of fitted values, residuals, covariance matrices and correlation sensitivity all pass. This is isolated committed-artifact execution using installed dependencies, not a fresh dependency installation, network clone or independent raw-processing replication.

No repair is needed for the adapter's declared scope. The original scientific conclusion remains a bounded pass on two exposed synthetic cases. Portable replay improves delivery of the numerical evidence; it adds no held-out case, measured hardware result, physical accuracy claim, confidence-coverage result or room-mapping evidence. Final remote-commit verification and clean GitHub delivery remain the coordinator's separate checks.

Review evidence is retained in `work/review-joint-reference/portable-check.py`, `portable-check.json`, `portable-check.log`, `portable-isolated.log`, `portable-isolated-result.json` and the extracted `portable-clean/` package. The check script deliberately requires an unused extraction destination to preserve its evidence. This follow-up changed only its own work artifacts and this report; no runtime, original trial, index or HEAD mutation was performed.

Coordinator packaging note: exact follow-up source/output files are retained under [joint-reference-review](joint-reference-review/) with a separate portable checksum manifest.
