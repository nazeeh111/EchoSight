# Source-declaration admission and cancellation repairs

The [fresh bounded audit](../coverage-gap-audit-57e25b2.md) exposed native source-declaration contradictions entering ordinary mapping and unfinished geometry surviving cancellation. These are implementation defects, separate from undeclared physical changes or acoustic model mismatch.

Known source configuration/probe/playback-route contradictions now withhold geometry in ordinary mapping and baselines, withhold reference calibration, and withhold experimental multi-source inference per source session (raw and prepared paths). Separately calibrated source sessions may use distinct configurations. Missing declarations remain unknown/incomplete; no hardware authentication is inferred. Controlled acquisition uses the same source-declaration helper while retaining its stronger epoch/receiver controls.

Every numerical wrapper forwards cancellation. Late cancellation clears unfinished surfaces, hypotheses, dimensions and derived geometry covariance while retaining input observations/provenance. Legacy cancelled comparisons are separately guarded by the coordinator's support-identity repair.

The62focused regressions pass in34.260s. Exact file and dependency hashes are in verification.json. Checks enter through actual native raw packages, stored processing jobs, export/reload, multi-source prepared replay, reference calibration and real CLI SIGINT. New summary fields are checked against the result schema. The earlier single loopback denial was resolved by scoped permission and that HTTP test passed; it was not a numerical failure. Full assembled verification and independent review are subsequent gates.
