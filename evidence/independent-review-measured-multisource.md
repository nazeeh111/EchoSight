# Independent measured multi-source review

Reviewed portable source pinned by `evidence/measured-multisource-de8442b/verification.json`; every listed portable SHA-256 matched before checks. Full reviewed hashes are in `work/review-measured-multisource/reviewed-hashes.json`. Numerical core was immutable `de8442b2dd088c036d2b92eaeaf29a1273785795`, loaded from `work/measured-multisource/core/`, with module origins recorded. No main/evaluation/document files were edited. No complete inverse fit was rerun. The acceptance counterexamples below use stubbed results only, not acoustic fitting.

## Findings

**P1: none found in the bounded review.** The existing measured failure is reproducible evidence of a failed spatial target, not a successful physical benchmark. The limitations regarding pose discrepancy, array correlation, source behavior, metadata conflict and uncertainty are materially appropriate.

**P2, resolved after review: a frozen geometry-semantics acceptance rule was not enforced.** At `evaluation/dechorate_multisource.py:107–108`, the code checks plane counts, horizontal count, unmatched count and runtime but never applies `acceptance.forbid_enclosure_claim`. An isolated execution of the actual `fit()` scorer with five exact matching reference planes, including a horizontal plane, reports the joint row as passing even when those surfaces are marked `extent_status='known_physical_extent'` and `mesh_semantics='closed_room_boundary'`. These are existing output field names; the counterexample deliberately supplies values inconsistent with their allowed semantics. A second legacy top-level `enclosure_status='closed'` counterexample also passes, but the recommended correction should be based on the current contract, not on an invented result flag or keyword scan.

The minimal existing-contract predicate is that returned reflector surfaces keep `extent_status == 'unknown'` and `mesh_semantics == 'reflection_support_convex_hull_not_physical_edges'`, as defined in `schemas/result.schema.json` and emitted by the core. Apply this when the frozen rule is enabled and record a specific acceptance failure. No matching thresholds or historical criteria need change. Add a scorer-level counterexample and re-score existing saved outputs; no21-fit rerun is needed. All actual recorded surfaces already have those allowed semantics, so this omission does not change the published failed outcome. Executed evidence: `semantic_checks.py`, `semantic-checks.json`, and the isolated `acceptance-stub/` outputs.

**P2: explicitly document and pin the dataset-specific receiver coordinate interpretation.** `evaluation/dechorate_multisource_diagnostics.py:18–21` verifies only Cartesian/metre attributes before treating `ReceiverPosition` as room coordinates, and the audit document calls the coordinates consistent without explaining the frame exception. The actual files have nonzero `ListenerPosition` and nondefault `ListenerView`; generic receiver coordinates in SOFA are listener-relative, so units/type alone do not justify that interpretation.

The numerical interpretation used here is supported by the publisher's converter, not by a generic SOFA assumption. The pinned converter directly assigns `mic_pos[:,mic_idxs].T` to `ReceiverPosition` while also assigning `arr_pos` to `ListenerPosition`. I independently verified its Git blob SHA-1 `cd03ca06621e64f217599a811e56ac0701b98760` against the existing tree for commit `d3e664f1e7a7d46241d7f7b3b3761448686b9537`. All forty stored receiver positions exactly equal the corresponding WORLD microphone annotation coordinates; all source/array positions also match their annotation fields. Annotation-file SHA-256 matches the manifest. Only geometry fields were read; no echo annotations were loaded. Thus do not apply a new listener transformation or refit the benchmark. Document the explicit publisher-layout exception, converter hash and equality checks so future reuse cannot silently transform the data twice. This affects the echo-refined diagnostic interpretation, not the primary beacon-derived fitting inputs. Evidence: `data-and-input-checks.json`, `semantic-checks.json`.

Primary references checked narrowly: [publisher converter at the pinned commit](https://github.com/Chutlhu/dEchorate/blob/d3e664f1e7a7d46241d7f7b3b3761448686b9537/dechorate/main_build_sofa_database.py), verified from the existing local source/tree because a fresh raw-file web fetch returned a cache miss; [SOFA coordinate-system description §3.2](https://sofaconventions.org/data/amt-1.0.0/sofa/doc/SOFA%20specs%200.6.pdf), describing the global versus listener-relative distinction; [the SingleRoomSRIR convention table](https://www.sofaconventions.org/mediawiki/index.php/SingleRoomSRIR), including the actual file convention1.0. The current AES web PDF inspected additionally was a2026DRAFT, not an adopted standard; it is not used as normative authority in this review.

## Independent checks that passed

- Hash/length verified all eight selected SOFA files and pinned CSV/measurement expansion source. Inspected actual metadata: all IR arrays have shape1×5×48000,48kHz, finite values and zero `Data.Delay`; full embedded MIT notices are present. File titles correspond to selected filenames. The contradictory room description is real and already disclosed. `RoomTemperature=0` is a default/missing temperature value, not evidence for the separately supplied speed346.98m/s.
- Independently convolved the complete forty one-second measured IRs with the exact probe and prescribed global gain. All generated Float32 sample arrays equal the stored WAVs exactly. All forty stored recording hashes match the processed observations; the immutable decoder preserves every Float32 value exactly as Float64. This checks actual source/channel ordering through recording import, not just filenames.
- Executed the inspected publisher rotation function on the original beacon CSV and its capsule-offset expression. Expanded array5/6 receivers match the harness to numerical precision. Source survey coordinates and the +2.355m coordinate-origin translation agree with the pinned source. No echo-refined geometry enters the primary sessions.
- Re-extracted exactly one representative imported recording through immutable `signals.py`; candidates, clock diagnostics and full signed response exactly match the saved observation. The remaining39were checked at waveform/import/hash level. No claim is made that this is an independent replication of all40signal fits.
- Recomputed all21 existing result matching counts using an independent assignment implementation with the frozen5°/15cm gates. True/unmatched/missed/horizontal counts agree exactly. Every actual output retains unknown physical extent and support-hull semantics. Evaluated95%coverage booleans are suppressed as documented.
- Recomputed the path-diagnostic aggregate counts from their individual records. The78/240 versus163/240 primary compatibility counts and85removed-by-cap count are consistent; they remain geometric predictions, not measured echo labels. No uncapped candidates entered an inverse fit.
- Read the call chain and comparator imports: primary raw processing extracts once; grid, single-source and independent-consensus arms receive the same observations. The explicit immutable comparator-module loading addresses cached evaluation-package ambiguity. The saved whole-input hash is checked after all calls. Room-reference planes are created only after fitting, and the echo-refined arm is separately labeled as diagnostically dependent on acoustically refined poses.

Executed probe results are in `work/review-measured-multisource/{data-and-input-checks.json,semantic-checks.json,scoring-checks.json}`; scripts and logs are beside them. An initial reviewer-script import-path collision was corrected by loading the reviewed harness by explicit file location; it did not affect or modify the benchmark. The first import error is not a benchmark defect.

## Limits and disposition

This audit does not establish room/furniture ground truth beyond the supplied six-plane reference, validate a single acoustic emitting center, prove survey covariance, independently authenticate historical freeze timing, or turn hybrid replay into own-device validation. Source labels and repeated source poses do not certify constant orientation or one-driver behavior. No additional downloads or hardware actions were performed.

The core's failed recovery and shuffled-geometry false surfaces stand. The coordinator fixed the missing acceptance rule without changing frozen targets. I read the new `acceptance_failures`, independently re-applied it to all21saved outputs, and confirmed every original pass/fail decision unchanged. The3focused unit checks pass. The new implementation also applies the runtime limit to controls. Reviewed updated hashes and my independent recheck are in `reviewed-hashes-after-fix.json` and `acceptance-fix-check.json`. No inverse fitting was needed. The coordinate exception documentation remains the only requested clarification; no numerical correction is indicated.

## Proposed coordinate clarification for the coordinator

These selected dEchorate SOFA files use the publisher's dataset-specific coordinate layout: `ReceiverPosition` contains room-frame microphone coordinates even though `ListenerPosition` is nonzero. The pinned [publisher converter](https://github.com/Chutlhu/dEchorate/blob/d3e664f1e7a7d46241d7f7b3b3761448686b9537/dechorate/main_build_sofa_database.py) directly writes annotation `mic_pos` into `ReceiverPosition`, annotation `arr_pos` into `ListenerPosition`, and annotation `src_pos` into `SourcePosition`. Its Git blob is `cd03ca06621e64f217599a811e56ac0701b98760`; SHA-256 is `254dc40b0739944ae727a69e1d6a69288cd59d23d078324593fc1c03d2879631`. An independent check found all40stored receiver coordinates exactly equal the corresponding room-frame microphone annotation coordinates and verified the annotation file against its frozen hash. Therefore this dataset adapter deliberately does not apply a listener-relative transform. This is a publisher-layout exception, not a general SOFA rule. It concerns the echo-refined pose diagnostic; primary fitting still uses the independent beacon CSV expansion.

Coordinator integration note: the dataset-specific coordinate exception and exact converter hashes are now documented in [MEASURED_MULTISOURCE.md](../docs/audit/MEASURED_MULTISOURCE.md). The scorer fix, three passing tests and independent unchanged21row recheck are retained beside this review. No coordinates or frozen thresholds changed.
