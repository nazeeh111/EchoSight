# Frozen v2 search follow-up

V1's twelve development results and post-fit true-parameter diagnostic remain untouched. V1 found no source flags, so it removed no false planes. The evaluation-only true offset substantially improved waveform residuals, showing a search limitation; truth must never initialize v2.

V2 reuses the identical development raw bytes, processed responses, kernels, training/withheld split, nuisance terms and engineering thresholds. It changes only search initialization and search implementation, identically for rigid-secondary and fixed-near-plane competitors. This is development iteration, not an independent held-out result.

Precompute each training record's full signed-waveform projected squared residual over delays−2..+2ms in1sample steps; every delay uses the same five whole-mixture alignments and two linear gains as v1. Outside this delay range, use the single-component residual. Generate4096 deterministic unscrambled Sobol points in each model's frozen four-dimensional parameter box, require physical validity for the fixed plane, rank only training residuals and refine the20best starts for at most70evaluations each against interpolated delay-profile residuals. Refine at most three resulting distinct best parameter vectors against the full waveform objective for at most70evaluations each. Identical counts apply to both model classes. Keep the single-component baseline unchanged. Save the initial-grid minimum, per-stage costs, objective evaluations and wall time. No labels, generating offset, true planes or held receiver waveform enters search.

Retain all v1 acceptance and no-promotion requirements. Report whether v2 identifies the secondary component, predicts held responses better than both competitors, preserves all qualified controls, and whether source-flagging withholds true geometry as well as removing false geometry. A successful result requires later shared-pose/clock nuisance sensitivity and independent review before any runtime promotion. Parameter-search success is not calibrated physical source detection.
