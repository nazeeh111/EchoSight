# Frozen v3 physical-admission and component-support correction

V1 missed every two-emitter case. V2's label-free wider search detected four but falsely rejected a real near-reflector, withholding seven true surfaces. Preserve both outcomes and their exact sources. V3 keeps the same waveform residual threshold, nuisance limits, profiles, bounds,4096initial candidates,20profile refinements and3raw refinements. Both alternatives still use identical raw inputs/search budgets.

Corrections: plane reflection contributes zero on each physically absent source/receiver path, while the primary direct component remains. Do not assign a blanket bad-fit penalty because one held receiver is on the opposite side. A source-model flag additionally requires at least three strong secondary-component observations (absolute fitted gain>.1) at EACH of four supplied source poses in withheld waveform prediction. Center those four source positions and require the smallest singular value to exceed max(30mm,6times declared source-position SD). This is a conservative engineering conditioning gate, not proof of identification or a calibrated probability. Source poses without an audible component cannot establish diversity. Constant source orientation is required by this experiment; arbitrary device rotation or independent driver delays remain outside this model.

Verify absent-path zero contribution and same-side geometry analytically. Replay the original twelve development cases without changing their raw/processed bytes or prior results. Independently render twelve fresh DEVELOPMENT cases at seeds2423/2437, the same six families and frozen renderer parameters, before any v3 fit. No previous frozen held-out answers enter fitting. This is development evidence, not a new held-out certification.

Stop criteria: any source flag on a qualified single-source/near-reflector/null/filter control, failure to retain all seven baseline-recovered near-reflector surfaces, or no dual-source benefit on the fresh cases closes this diagnostic branch without a fourth search/gate iteration. Preserve incomplete/no-result baseline cases as baseline limitations. If the gate works, quantify source-offset error, waveform prediction, source-calibration sensitivity, clock/rejection limitations, runtime and all true geometry withheld. Clearing all geometry after a source flag converts every baseline matched true surface to a miss; report baseline and guarded totals rather than counting removed false planes alone.

No runtime/API changes or promotion follow automatically. Any positive recommendation needs independent review, shared covariance sensitivity and explicit value versus calibration burden. This experiment is a possible source-qualification request, not a corrected mapper or established multi-driver acoustic calibration.
