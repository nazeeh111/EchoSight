# Portable replay and preflight review

No actionable findings in the reviewed helper or portable adapter.

The archived replay passes all 20 saved summary comparisons under the pinned reproduction environment. Transfer and promotion remain false. All ten room-method complete-RMS and complete-max labels are null; favorable subset statistics remain explicitly labeled unique-only.

The helper's inclusive ideal-candidate rule was checked independently against exact rational arithmetic on 512 three-path catalogs, including identical arrivals and the boundary. The difference between individually unique ideal arrivals and disjoint uncertainty windows is correct: separations greater than one radius may pass the ideal catalog while windows still overlap until greater than two radii. This output is not a claim that noisy or physical extraction is feasible. Completeness is checked against the explicitly declared path identifiers; the documented limitation that undeclared physical paths cannot be detected is necessary and accurate.

All 24 archived physical excess delays equal the original frozen truth differences. The helper returns 20/24, false; the four conflicts are at captures 14 and 15. The CLI returns exit 1 and saves the diagnostic. Attempts to overwrite that output or the input fixture fail and preserve all bytes. Function inputs remain unchanged. Five focused unit tests passed. The independent review checked all relevant source/evidence hashes before and after, with no changes.

Commands (from main repository):

```sh
PYTHONDONTWRITEBYTECODE=1 /Users/nazeeh/Documents/Codex/2026-09-19/goal-deliver-echosight-s-verified-backend/work/reproduction-venv/bin/python evidence/calibration-room-transfer/replay/replay.py --repo "$PWD" --output work/transfer-evaluation-review/portable-check.json
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m unittest tests.test_held_path_preflight -v
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python work/transfer-evaluation-review/check_preflight.py
```

The review script intentionally expects fresh output names; preserved receipts block accidental overwrites. Full hash bindings are in preflight-review.json. No mapper, calibration, raw extraction or timing measurement was repeated, and no production files were edited. Review remains bound to these file hashes until the assembled commit is identified.
