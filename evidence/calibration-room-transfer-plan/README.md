# Next calibration-to-room experiment, unexecuted

This is an exact prepared experiment, not a production feature or measured result. The original protocol, settings, runner, static-check and freeze bytes are preserved. Freeze SHA256: `a995d28014c4c055003d34db193f5657a08d185a281d09ff1d7e667e5405da40`, runtime `5d6423486be5fd448a4d7b42fbbaee264eb07c9c`.

The retained reference recordings have only an isolated reference reflection, so they cannot establish room/height transfer. Existing room recordings have a different source/effective-speed configuration. This missing matched data is a scientific limit, not a reason to invent a public calibration feature. The two proposed room realizations and two null controls remain ungenerated, following the latest instruction to avoid new datasets during completion.

The [protocol](PROTOCOL.md) fixes source continuity, independent mapping survey/audio errors, frozen comparisons, all six planes including height, zero unmatched surfaces, 5°/0.10m geometry, four withheld views with 100µs RMS/200µs maximum, and the 10s processing target. Every failure and comparator remains in the denominator. Promotion also requires the predeclared gain in both instances. No physical qualification follows.

Only syntax, settings, source metadata,48 retained WAV hashes and17 runtime files were checked. The runner has not rendered or processed recordings; its end-to-end behavior is unverified. Future execution needs review of the freeze and the declared data scope, then restoration under `work/calibration-room-transfer/` with the existing retained V2 inputs and exact runtime. Running it directly from this archive changes its root lookup and is unsupported. Do not alter a freeze to accommodate later code.
