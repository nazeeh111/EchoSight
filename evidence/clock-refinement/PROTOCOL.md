# Frozen development clock-refinement comparison

This isolated experiment compares immutable de8442b acquisition with one label-free additional trigger for its existing rate-matched template retry. No main edits, oracle fitting input, source-model gate changes or reserved seeds2609/2621. Original v4 observations, source-warning counts and snapshot remain unchanged. The postfreeze oracle addendum is separately archived and hashed before this study.

## Candidate algorithm fixed before execution

Keep the existing failed-affine-fit trigger and one bounded retry. Additionally retry an accepted coarse train when its observed within-pulse clock displacement `abs(alpha_est-1)*pulse_duration_s` exceeds the existing half-bandwidth timing floor `0.5/(high_hz-low_hz)`. For this40ms/12kHz probe that means about1042ppm. This engineering trigger derives from nominal temporal resolution; it is not a probability threshold or identifiability proof. Use only the observed coarse alpha to stretch the acquisition pulse. Keep its existing±1ms nearest-pilot search, require smaller maximum affine residual, and keep all subsequent100µs/5000ppm rejection, weak-direct, resampling, anchoring, candidate and resource/cancel behavior. Exactly one retry, no iterative convergence tuning. Record initial/refined alpha and residuals for convergence diagnosis. Nominal alpha and alternative alpha are not used to alter the waveform generation or evaluation truth.

## Equal raw inputs

A. Reuse all1152recordings comprising the12fresh v3 cases and12fresh v4 raw-clock cases. Verify their manifests and saved baseline extraction exactly. Run both immutable baseline and candidate through raw input on every recording. No truth/scene annotation enters either processing call. Preserve all rejected cases. Retain previous source-waveform assumptions and surveys by reading unmodified sessions. No mapping or source-warning fits are needed for this acquisition comparison.

B. Independently render96targeted development recordings before fitting: seeds2701/2713, nominal relative clock ppm[-4500,-1800,-800,0,800,1800,3500,4500], six families: direct-null, separated-plus-overlapping multipath, weak physical direct, nonlinear source waveform, smooth nonlinear clock and step clock. Standard48kHz,40ms2–14kHz chirp,7repeats350ms apart. Direct4ms; normal gain.45. Multipath adds relative delays[1.2,4.4,4.5,12]ms with gains[.24,.18,.12,.10], explicitly retaining100µs overlap. Weak-direct amplitude.018 with stronger echo.45at+6ms and.18at+12ms. Nonlinear waveform uses tanh(3*x)/3 on the direct-plus-multipath waveform before sampling. Smooth clock adds0.8ms*sin(2*pi*u/2.5s) to receiver time; step clock adds1ms after source-buffer time1.2s. Other families are affine. Record offsets uniform20–100ms, additive noise SD.00005, fixed16-bit quantization, no clipping renormalization. Fractional delays use49tap windowed-sinc. True alpha, source time-to-receiver time and path labels remain evaluation-only. Seeds differ from prior source studies and reserved acceptance seeds.

## Metrics and fixed decision criteria

Report per-record admission/rejection changes and diagnostic codes; accepted affine rate error against generating alpha and error/reported alpha SD; full candidate counts; one-to-one geometric delay matching at fixed75µs tolerance, false candidates and missed paths including unresolved overlapping truth; every nearest-path bias and conditional direct/candidate/rate uncertainty compatibility; local runtime. For nonlinear clock inputs, alpha is merely the nominal affine component, so do not present it as a true fitted target. Count any new accepted weak-direct/warped control and inspect direct reference rather than calling acceptance a success. Report original direct-null false candidates separately. In targeted controls, admission is not a geometry claim.

The candidate earns a production-fix recommendation only if it reduces accepted affine large clock errors (absolute error>50ppm) and false direct-null candidates, with no newly admitted weak-direct/nonlinear-clock/step-clock controls, no loss of valid affine admission, no increase in aggregate false candidates or missed truth paths on common-accepted valid affine inputs, and maximum per-record runtime below2seconds. These are conservative development gates, not frozen room-mapping acceptance replacements. Any failed gate closes promotion for this candidate; finish all predeclared cases and preserve failure evidence. Do not modify triggers, rejection thresholds or search to repair these answers. If benefits and losses coexist, report both and identify the limiting mechanism. No hardware validation or calibrated95%coverage claim follows.

Freeze protocol, renderer, candidate diff, runner and source hashes before new generation/fitting. Preserve raw manifests and code versions. Postfit oracle data must never be read by the candidate. No full-scene mapping rerun or additional family expansion without separate bounded justification.
