# Continued-work instruction

The user explicitly reopened the project after baseline commit `1c77336`: stop treating current implementation as nearly complete; treat it as a baseline research prototype. Re-audit the entire original charter requirement by requirement, linking implementation claims to code and executed evidence. Distinguish implemented, partial, missing, simulation-only, external measurements and awaiting our hardware.

Continue autonomous implementation in this order: physical correctness; spatial inference; real recording path; product behavior; harder independent evaluation; delivery. Challenge the current method against serious alternatives with equal inputs. Preserve frozen criteria, failures and misses. No sophistication without measurable benefit, no software-only physical-accuracy claim, no optimistic completion percentage. After each milestone investigate the strongest remaining failure and implement the most valuable justified improvement.

Complete all tractable charter gaps. For requirements requiring our devices, finish independent work first and provide the exact hardware experiment, acceptance threshold and consequence of failure. Report software coverage, synthetic validation, external measured-data validation, own-device validation and unresolved science separately. Keep specialist work active on concrete independent tasks and push coherent verified checkpoints frequently. The user's repeated emphasis is complete evidence-based work from every angle, not an early delivery declaration.

Original authority and scope remain in [CHARTER.md](CHARTER.md). No deployment, spending or global changes are newly authorized. Native goal remains active.

## Later-stage steering, 2026-09-19

Continue the existing implementation and outstanding review repairs; do not restart or redesign. After the existing acoustic geometry/output stage, preserve a later material-analysis stage using available reflection, absorption and spectral evidence, producing material probabilities only when supported and using existing/local ML where appropriate. A separate subsequent appearance/color stage may estimate plausible real-world appearance from material, geometry and context. Acoustic frequency is not an optical-color measurement. Keep acoustic false-color display separate from predicted appearance.

Preserve the requested five-perspective/agent reasoning and final adjudication stage when it becomes relevant. Its outputs must remain attributed interpretations and cannot overwrite raw acoustic evidence, geometry or their uncertainty. Retain established perspective definitions when that stage is reached; do not invent missing roles or claim this layer is implemented. The current specialist implementation team is not evidence that product-level adjudication exists.

These stages are deferred, not placeholders to insert prematurely. Retain unknown/unsupported states. Existing immutable raw recordings, result IDs, surface IDs and candidate-level evidence references are the integration foundation; no new runtime dependency, model service, probability field or appearance output is added by this steering. When justified implementation begins, its contracts, data requirements and validation must distinguish acoustic inference, material hypotheses, contextual appearance predictions and advisory adjudication.

Before substantial changes, require a concrete benefit to the real backend. Prefer the smallest cohesive implementation preserving behavior. Inspect duplication, stale/dead code, oversized functions, unexplained thresholds, schemas, hidden fixture assumptions, silent fallbacks, unsupported confidence and tests that mirror implementation. Remove bloat only when safe and evidenced; do not rewrite working code for style. Priority remains correctness, connected end-to-end behavior, robustness, provenance, testability, clarity and efficiency. No new spending, external publication or deployment authority is implied.

## Verification and simplicity steering, 2026-09-19

The latest instruction preserves the exact active path: finish cancellation, source-admission, clock and calibration work with independent review, then choose the highest-impact recording-to-3D failure by expected measurable benefit. A defensible assessment requires all tractable charter obligations verified and no known tractable P1/P2 defect deferred. Do not optimize a cosmetic score, reopen closed branches without new evidence, retune exposed cases or weaken frozen criteria.

Each checkpoint must separately report software implementation coverage, synthetic validation, external measured-data validation, own-device validation, unresolved science, and code-quality debt introduced or removed. Keep one authoritative implementation of each public policy across API, CLI, jobs, replay, calibration, multi-source processing and schemas. Consolidate duplicate policy only where it reduces demonstrated risk; preserve behavior with meaningful boundary tests. Passing tests remain software evidence.

The deferred interpretation order is acoustic evidence → geometry → material probabilities → plausible appearance probabilities → adjudication. Each later layer requires sufficient evidence, explicit provenance and unknown states. Acoustic false color remains distinct from plausible appearance; agent reasoning cannot overwrite raw evidence or numerical results. These additions do not accelerate their implementation ahead of reliable geometry.

Completion still requires a clean GitHub checkout, full relevant tests/demos, exact contracts/examples, fresh independent assembled review, verified remote commit and resolution of known tractable material defects. Remaining hardware dependencies require exact experiments, thresholds and failure consequences after independent work is finished.
